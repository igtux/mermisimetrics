# mermisimetrics

Owner: @admin  ·  Host: host1  ·  Path: ~/projects/mermisimetrics
Remote (canonical): git@github.com:igtux/mermisimetrics.git
Remote (configured here): git@github-gitman-admin:igtux/mermisimetrics.git
Branch: main  ·  Managed by Jarvis Gitman.

## Push / Pull

The host has a Gitman SSH key wired to a unique alias `github-gitman-admin` in
`~/.ssh/config`, so `git push` / `git pull` from inside this directory just work.

## Sync from GitHub

`git pull --rebase`

## See this project in Jarvis

Agents → Gitman → "mermisimetrics"
